{% extends 'base.html.twig' %}

{% block title %}Reponse
{% endblock %}

{% block body %}
	<h1>Reponse</h1>

	<table class="table">
		<tbody>
			<tr>
				<th>Id</th>
				<td>{{ reponse.id }}</td>
			</tr>
			<tr>
				<th>Message</th>
				<td>{{ reponse.message }}</td>
			</tr>
			<tr>
				<th>PostedAt</th>
				<td>{{ reponse.postedAt ? reponse.postedAt|date('Y-m-d H:i:s') : '' }}</td>
			</tr>
		</tbody>
	</table>

	<a href="{{ path('message_index') }}">Retour à vos messages</a>


	{{ include('reponse/_delete_form.html.twig') }}
{% endblock %}
